import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Icon = () => {
  return (
    <View>
      <Text>Icon</Text>
    </View>
  )
}

const styles = StyleSheet.create({})

export default Icon;